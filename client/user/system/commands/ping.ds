Option Explicit

Dim tmp
tmp = Ping(ArgV(1))
Say "Temp = " & tmp
Dim Result
Result = WaitFor(tmp)
Say "Result = " & Result

If Result Then
    Say "{{green}}Server " & ArgV(1) & " is online."
Else
    Say "{{red}}Server " & ArgV(1) & " is offline."
End If
