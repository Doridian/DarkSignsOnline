Option Explicit

Dim Result
Result = WaitFor(Ping(ArgV(1)))

If Result Then
    Say "{{green}}Server " & ArgV(1) & " is online."
Else
    Say "{{red}}Server " & ArgV(1) & " is offline."
End If
