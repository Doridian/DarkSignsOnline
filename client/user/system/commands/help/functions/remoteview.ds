Option Explicit
Say "{{green 12}}Function: RemoteView(Domain[String], filename[String], <StartLine[Long,Default=0]>, <MaxLines[Long,Default=0]>) -> HTTPRequest"
Say "{{lred}}Restriction: Can only be used locally (local script or CLI)"
