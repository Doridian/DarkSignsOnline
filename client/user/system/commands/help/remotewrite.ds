Option Explicit
Dim props, propsforexamples
props = "{green 12 underline}"
propsforexamples = "{lgreen 12}"

Say props & "Command: REMOTEWRITE domain file data here"
Say propsforexamples & "Example #1: REMOTEWRITE matrix.com Hello World"
Say "{lgrey}Write some data to a file on your domain name file system."
Say "{lorange}You must own the domain, or have subowner privileges on the domain."
