Option Explicit

Sub SaySlow(Delay, Str, Style)
    Dim X
    Style = "{" & Style & "}"
    Say Left(Str, 1) & Style
    For X = 2 To Len(Str)
        Wait(Delay)
        SayLine Mid(Str, 1, X) & Style
    Next
End Sub
